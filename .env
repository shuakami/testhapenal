# 演示用途的环境变量（生产环境请使用安全方式注入机密）

# 应用名称（会显示在 OpenAPI 文档中）
APP_NAME=FastAPI + SQLite（Compose 教学示例）

# 运行环境
APP_ENV=dev

# SQLite 连接串（使用 aiosqlite 异步驱动；注意 4 个 / 表示容器内的绝对路径）
DATABASE_URL=sqlite+aiosqlite:////data/app.db
